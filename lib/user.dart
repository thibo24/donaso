class User {
  String nom;
  String prenom;
  String mail;
  String password;
  String username;
  int points;

  User(this.nom, this.prenom, this.mail, this.password, this.username,
      this.points);
}
